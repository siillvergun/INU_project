spring:
  config:
    import: optional:file:.env[.properties]
  profiles:
    active: local

app:
  cors:
    allowed-origin-patterns: ${FRONTEND_ORIGIN_PATTERNS:http://localhost:5173,http://localhost:4173,https://*.vercel.app}
